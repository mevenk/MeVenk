import { ApplicationHTTPInterceptor } from './application-httpinterceptor';

describe('ApplicationHTTPInterceptor', () => {
  it('should create an instance', () => {
    expect(new ApplicationHTTPInterceptor()).toBeTruthy();
  });
});
