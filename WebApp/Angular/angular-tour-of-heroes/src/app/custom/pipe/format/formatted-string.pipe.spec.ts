import { FormattedStringPipe } from './formatted-string.pipe';

describe('FormattedStringPipe', () => {
  it('create an instance', () => {
    const pipe = new FormattedStringPipe();
    expect(pipe).toBeTruthy();
  });
});
